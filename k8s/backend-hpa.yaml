apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agricloud-backend-hpa
  namespace: agricloud
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agricloud-backend
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 45

